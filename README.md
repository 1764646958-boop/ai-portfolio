# 李倍旭 · AI 应用作品集（AI Portfolio）

> **把模糊的业务问题翻译成可验证、可交付、可复制的 AI 系统，并对效果与 ROI 负责。**

以「AI 应用落地」为主线的实践记录：从业务一线做机会诊断（业务漏斗 / SIPOC / ROI 测算），到亲手交付可运行、可评测、可复现的系统。
本仓库按 **决策叙事六段式** 组织每个作品：`业务问题 → 为什么用 AI → 架构与护栏 → 评测与复盘 → 指标 → 角色边界`。

📄 **作品集 PDF（公开版 · 仅邮箱）**：[`00_作品集总览/李倍旭-AI作品集（2026-09·公开版）.pdf`](00_作品集总览/李倍旭-AI作品集（2026-09·公开版）.pdf)

---

## 目录

| # | 作品 | 类型 | 我的角色 | 关键指标 | 仓库内 |
|---|---|---|---|---|---|
| ① | 商品智能导购 Agent（澄初个护） | 交付 · 独立完成 | 全栈 + 决策人 | 16/16 场景、BC-1~10 全过、断网可跑 | ✅ [`01_商品智能导购Agent（FDE08）`](01_商品智能导购Agent（FDE08）) |
| ② | WorkBuddy 开放 Agent 生态 · 资产质量与分发策略 | 策略 · 独立完成 | 策略设计主导 | 长尾命中实证（84 次调用 vs 51,200 次） | ✅ [`02_WorkBuddy生态策略（腾讯）`](02_WorkBuddy生态策略（腾讯）) |
| ③ | 《防线》DEFENSE LINE · AI 原生游戏 | 工程纵深 · 3 人团队 | LLM 服务 + Shader + 叙事 + 音乐 | Godot 工程 + 五端点 LLM 服务 + 四层记忆 | ⛔ 赛程评审中，暂不公开（可当面演示） |
| ④ | 卡旺卡门店 AI（实习） | 真实客户交付 | AI 优化岗（主导） | ROI 501.81% / 961.26%；L1-L4 验证 9/9、14/14 | ⛔ 含客户内部资料，按保密原则不上传 |
| ⑤ | 跨境 e 盾 · RegTech 合规风控平台 | 行业方案 · 团队 | 后端 Agent 核心 | 规则确定性决策 + LLM 仅做审计解释 | ⛔ 赛程评审中，暂不公开（可当面演示） |
| ⑥ | XGBoost 出品时间预测 | 数据评估 · 独立完成 | 独立建模 | MAE 3.27 分钟 / R² 0.6901 | ✅ [`06_XGBoost出品时间预测`](06_XGBoost出品时间预测) |

> ③④⑤ 未包含在本仓库：③⑤ 为仍在评审期的团队赛作品，④ 涉及客户内部业务资料。
> 三者均可在面试中**现场演示或按需提供脱敏版本** —— 对客户信息与团队材料守边界，本身就是这份作品集想表达的职业准则。

---

## ① 商品智能导购 Agent（澄初个护）

> **LLM 只做翻译，代码做决策。**

- **业务问题**：顾客说不清需求、怕被施压；门店要转化又要守广告法 / 功效宣称 / 医疗边界；品牌手册未提供的信息（促销 / 线上价 / 退换 / 授权）不得编造。
- **架构**：状态机 + 三大门控（敏感 / 政策 / 注入）先于生成；推荐与报价由确定性规则 + 知识库决定；
  **所有外发金额必须等于事实价或事实价之和**（子集和校验，如 `388 = P101 + P203`）；红线场景走硬模板、不调用模型。
- **评测**：`tests/test_scenarios.py`（16 个场景）+ `tests/test_bc.py`（10 条可检查行为 BC-1~10）全部通过；断网 / 无 API key 仍可完整运行。
- **运行**：

```bash
cd 01_商品智能导购Agent（FDE08）
python tests/test_scenarios.py   # 16 场景
python tests/test_bc.py          # BC-1~10
pip install -r requirements.txt
python -m uvicorn server:app --port 8000   # 或双击 start_demo.bat（含公网隧道）
```

**结构**：`rules.py`（确定性规则层）/ `brand_kb.py`（单一事实源）/ `nl.py`（LLM 翻译层 + 价格白名单校验）/ `agent.py`（编排）/ `server.py`（FastAPI 入口）/ `文档/`（复盘、演示脚本、SOP）

---

## ② WorkBuddy 开放 Agent 生态 · 资产质量与分发策略

> **让达标长尾资产有资格被看见。**

- **业务问题**：自然语言描述需求后连续 3 次匹配失败即放弃；仅 8% 创作者两周内获超 100 次调用；头部虹吸形成冷启动死锁。
- **分工第一性原理**：语义归模型、确定性归规则、不可逆决策归人。
- **交付物**：质量评估框架（双因子 + 双轨评分 + 贝叶斯收缩 + 三层门禁 + 五支柱采信）、10 阶段分发链路、可交互原型、效果指标与口径切换方案、马太效应抑制策略（加分项）。
- **验证**：原型内置「传统排序 vs 本框架」对照 —— 长尾需求「清蒸鲈鱼配什么清酒」按调用量排序命中 51,200 次调用的头部（几乎零契合），本框架命中仅 84 次调用的长尾；
  常数 `k / β / α` 由离线回溯脚本标定（`seed` 固定、可复现）。
- **运行**：双击 `可交互原型/index.html`（零安装，可切换真实 LLM）；校准脚本 `实现与校准/p1_backtest.py`。

---

## ⑥ XGBoost 出品时间预测

- 171 万条训练样本（时序划分）、24 个特征、308 家门店；排队特征用事件驱动算法 `O(N log N)` 精确计算。
- 外部测试集 **MAE 3.27 分钟 / RMSE 4.93 / R² 0.6901**，误差 ≤5 分钟覆盖 81% 订单；预订单独立建模使 MAE 30.92 → 9.90（-68%）。
- 商业判断：门店偏差校准 + P35 保守偏移（+1.1min，capped@3min），宁可高估也不让顾客等更久。

---

## 关于本仓库

- 全部指标与结论来自项目原始文档、测试输出与验证报告，**未作虚构**；团队与客户相关内容已按边界移除。
- 密钥类文件（`.env`）已通过 `.gitignore` 排除，仓库内只保留 `.env.example`。
- 结构索引见 [`00_作品集总览/作品集索引与结构说明.md`](00_作品集总览/作品集索引与结构说明.md)。

**联系方式**：231098011@smail.nju.edu.cn ｜ GitHub [@1764646958-boop](https://github.com/1764646958-boop)
