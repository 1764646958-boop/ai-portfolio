# -*- coding: utf-8 -*-
"""生成《马太效应抑制策略（生态治理·加分项）.docx》"""
import sys, os
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from _docx_helpers import *
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.enum.table import WD_TABLE_ALIGNMENT

doc = make_doc()
cover(doc,
    "马太效应抑制策略",
    "——WorkBuddy 开放 Agent 生态 · 生态治理（加分项）——",
    "在不损害长尾创作者可见性的前提下，识别并抑制“头部资产被反复推荐”的马太效应",
    [
        ["文档名称", "马太效应抑制策略（生态治理 · 加分项）"],
        ["交付类型", "任务要求加分项：识别并抑制头部反复推荐的马太效应"],
        ["文档定位", "方案（生态治理 / 反马太机制 + 作用信号 + 副作用控制）"],
        ["落点", "机制——P2 探索/利用与 P1 稀缺豁免；信号与副作用——P4 指标"],
        ["版本", "V1.1"], ["日期", "2026-09-09"], ["编制方式", "AI 辅助（Claude Code）"],
    ])

h1(doc, "摘要")
para(doc, "马太效应的本质是“只利用、不探索”：头部被反复喂，尾部永远饿着，形成一个**放大失衡的自洽闭环**。本策略把“识别—抑制”做成一个**动态闭环**："
     "先监测“马太是否在恶化”，再在分发层用【探索/利用 + 稀缺加权路由 + 重复曝光打散】加以抑制，最后用一组作用信号验证有效性、用护栏指标控制副作用。")
callout(doc, "核心机制一句话：把一部分分发给不确定性更高的“达标长尾”作为“试金石”，用平台能承受的代价反哺生态多样性。"
        "稀缺度因子作为路由偏好信号，在“合格基础上的最匹配优先”里给长尾一个公平的竞争力。", color=GREEN_600)
doc.add_page_break()

# 一、问题界定与识别
h1(doc, "一、问题界定：马太效应从哪来，怎么“识别”")
h2(doc, "1.1 根源")
bullet(doc, "任何基于历史行为的排序都会继续强化头部；头部因曝光多而数据多、数据多而更被信任、更被信任而曝光更多。", bold_lead="正反馈放大")
bullet(doc, "长尾“没数据→不敢推→更没数据”，能力达标也无法进入分发；平台缺少“给未经验证者一次机会”的机制。", bold_lead="冷启动死锁")
bullet(doc, "若质量分只依赖调用量 / 评分，则头部即使非当下最优也可能被反复推荐。", bold_lead="评分基于流量")

h2(doc, "1.2 识别信号（阈值 + 窗口 + 基线 四元组；越过即抗衡恶化）")
add_table(doc, ["信号", "统计窗口", "基线（≈现状）", "恶化阈值（触发抑制）", "方向"],
    [
        ["分发集中度（Top5% 占分发）", "近 14 天 / 滚动", "82%", ">88% 或 环比 +5pp", "↑=恶化"],
        ["头部重复推荐率", "近 14 天", "45%", ">70% 或 环比 +8pp", "↑=恶化"],
        ["长尾召回未命中率", "近 14 天", "60%", ">80% 或 环比 +10pp", "↑=恶化"],
        ["生态多样性 Gini", "近 14 天", "0.62", ">0.70 或 环比 +0.05", "↑=恶化"],
        ["覆盖断层（未命中但需求明确的簇）", "近 14 天", "2~3 簇", "新增 +20% 或 连续 2 周期存在", "存在=机会"],
    ], col_widths=[4.6, 3.0, 3.0, 4.4, 2.0])
para(doc, "这些识别信号由**规则**持续计算（可枚举阈值、窗口、基线、稳定、可审计），一旦越过恶化阈值即触发抑制层。"
     "它们不依赖复杂模型，因而可作为“低门槛、可信”的早期预警；窗口与基线在上线前用 P1 金标准与小流量试点重新标定。")
para(doc, "**与 P4 验收线对齐**：本表基线与《效果指标与口径切换方案》3.5 一致（长尾覆盖率基线 8%→目标 25%、Gini 0.62→0.50、Top5% 份额 82%→72%），"
     "保证“识别”与“验收”用同一套数值，不在两处打架。")

# 二、抑制机制设计
h1(doc, "二、抑制机制：识别 → 动态探索 → 稀缺加权路由 → 打散重复推荐")
h2(doc, "2.1 机制总览（分层）")
add_table(doc, ["层", "机制", "承担者", "说明"],
    [
        ["检测层", "识别信号越限即告警", "规则", "监测集中度 / 头部重复 / 多样性 / 供给缺口（用 1.2 四元组）"],
        ["探索层", "对小概率 / 高不确定性候选施加 UCB 乐观", "规则", "score_路由 = Q̂ + β·√(ln t / N)，β 可控"],
        ["稀缺加权路由", "同质需求上，给“稀缺且合格”的长尾更高的生态增益", "规则 + 模型", "生态增益 = 1 + α·(稀缺度×需求缺口)，α 封顶调优"],
        ["打散重复推荐", "同一头部资产不因“凑合相似”连续霸占不同需求", "规则", "对头部设每需求簇推荐上限 / 冷却期，腾出曝光位"],
        ["稀缺豁免", "内容先验合格即条件放行进探索池", "规则 + 人工复核", "防误杀达标长尾，防稀缺被拆分滥用"],
    ], col_widths=[3.2, 6.0, 2.6, 4.2])
h2(doc, "2.2 在不损害长尾可见性前提下的克制")
para(doc, "本策略**优先保护长尾可见性**：探索预算保证长尾有首次曝光；稀缺加权让“恰好契合某需求”的长尾能公平竞争；打散重复推荐从头部手里“腾”出曝光位而非削减总量。"
     "它不“拉低头部”，而是**给长尾一个对等的起跑线**——因此不会以牺牲长尾为代价来抑制头部。")

# 三、核心公式与调优
h1(doc, "三、核心机制：Explore / Exploit + 稀缺偏好")
para(doc, "在 P2 阶段⑥，对每个候选计算“路由分”，权衡“利用已知最优”与“探索未知长尾”：")
callout(doc, "score_路由 = Q̂ + β · √(ln t / N)\n"
        "最终分发 = score_路由 · (1 + α·(稀缺度 × 需求缺口))，α 封顶")
add_table(doc, ["参数", "含义 / 取值", "调优原则"],
    [
        ["β（探索系数）", "β=0.15（中等偏保守；与 P1 符号表一致）", "随“探索池命中率”做自适应调节，命中率低则下调"],
        ["α（稀缺权重）", "α=0.15（默认）／膝点 0.2（多样性最优）", "只抬升“合格之上”的稀缺，封顶防反超劣质"],
        ["探索预算", "每窗口保留 10% 给探索池；新资产保底 250 次（与 P1 一致）", "据护栏指标回调"],
        ["头部冷却期", "同一头部在同类需求上的重复曝光间隔，按需求簇密度设定", "防连续霸占；从不削头部总曝光"],
    ], col_widths=[3.6, 5.4, 6.0])

# 四、作用信号
h1(doc, "四、作用信号（怎么判定策略起作用）")
para(doc, "“起作用”必须由**可观测、可对照**的信号判定，且给出**对照方式、显著性门槛与最小提升量**，而非主观感受：")
add_table(doc, ["作用信号", "对照方式", "最小提升量 / 显著性", "对应策略"],
    [
        ["探索池长尾命中 / 续用率", "长尾探索组 vs 头部对照组（DID）", "领先 ≥5pp 且 p<0.05", "探索层"],
        ["长尾曝光覆盖率", "上线前 / 上线后两期对照", "Δ ≥ +17pp（反 8%）", "稀缺加权 + 豁免"],
        ["生态多样性 Gini", "上线前 / 后两期对照", "Δ ≥ −0.05（Gini 降）", "整体机制"],
        ["头部平均份额", "上线前 / 后两期对照", "Δ ≥ −10pp", "打散 + 探索"],
        ["头部重复推荐率", "上线前 / 后两期对照", "Δ ≥ −8pp", "打散重复推荐"],
        ["长尾召回未命中率", "上线前 / 后两期对照", "Δ ≥ +10pp（终于命中）", "稀缺豁免 + 探索"],
    ], col_widths=[4.6, 5.0, 6.0, 3.4])
para(doc, "判定逻辑：**上述信号需联合看**，且必须结合护栏指标一起下结论——若长尾覆盖率上升但满意度/头部体验退化，说明探索过度，应回调 β 与预算；"
     "任何“起作用”的结论都必须先过 DID 归因（剔除口径切换与抽样波动），避免把波动当成效。")

# 五、副作用控制
h1(doc, "五、副作用与护栏（黄 / 红 分级量化红线）")
para(doc, "副作用的抑制不靠“感觉”，而靠**黄（预警回调）/ 红（熔断回滚）**两级量化红线：")
add_table(doc, ["副作用", "黄线（预警，回调 β/预算）", "红线（熔断，回滚策略）", "控制机制"],
    [
        ["探索过度伤体验", "匹配采纳率较基线 −3pp 或满意度 −0.2", "采纳率 < 基线 −10pp 或满意度 < 4.0", "回调 β 与探索预算；红则回滚"],
        ["稀缺因子被拆分滥用", "疑似拆分告警 ≥5 次/周期", "实锤伪造 ≥3 次或触发 1 次创作者争议", "语义聚类去重 + 人工复核；稀缺仅乘性封顶"],
        ["人工复核成瓶颈", "人工累计复核 >500 次/周期", "单点响应 >72h 未清", "仅稀缺豁免与清退留人，常态自动 + 扩容"],
        ["长尾加入但质量参差", "探索池命中率 < 25%", "探索池命中率 < 15%", "收紧稀缺豁免与底线闸门，确保“达标”长尾"],
        ["头部反弹 / 创作者流失", "头部创作者月流失 +10%", "头部参与创作者流失 >15%", "保留头部在高契合需求上的主导，只打散“凑合相似”曝光"],
    ], col_widths=[4.6, 4.6, 4.6, 6.2])
para(doc, "黄线触发→自动回调（降 β、缩预算、升人工抽检率）；红线触发→**立即熔断并回滚**，上报治理委员会复盘，同时保留“给长尾起跑线”的机制不变，只调干预强度。")

# 六、闭环
h1(doc, "六、自适应闭环与策略退出")
para(doc, "策略是**动态闭环**而非一次性开关：")
bullet(doc, "用检测层识别信号监测马太状态；用探索/稀缺/打散抑制；用作用信号判定有效；用护栏（黄/红）控制副作用。", bold_lead="识别→抑制→验证→护栏")
bullet(doc, "当生态多样性达到稳态目标、长尾覆盖达标后，逐步**回调** β 与探索预算，让策略从“强干预”过渡到“轻维持”，避免长期过度探索。", bold_lead="策略退出")
bullet(doc, "整套策略本身也要接受 P4 的口径切换，保证评估口径与干预强度联动。", bold_lead="与 P4 联动")
para(doc, "**退出判据（量化）**：长尾曝光覆盖率 ≥25%、Gini ≤0.50、头部份额 ≤72% 且持续 4 个统计周期，即可把 β 从 0.15 逐步降到 0.05、探索预算从 10% 收到 5%，只保留“轻维持”探索。")

h2(doc, "6.1 与各产出物的关系")
add_table(doc, ["产出物", "本策略承接 / 依赖"],
    [
        ["P1 质量框架", "依赖其稀缺度、需求缺口、门禁、金标准锚点；并把“稀缺豁免”作为抑制机制之一"],
        ["P2 分发链路", "落实在阶段⑥探索/利用与阶段⑦稀缺豁免，作为反马太的工程载体"],
        ["P4 指标方案", "作用信号与护栏指标在此收束，并与稀疏/稳态口径切换联动"],
    ], col_widths=[4.6, 11.0])

h1(doc, "七、结论")
para(doc, "本策略以“检测—探索—稀缺加权—打散重复—稀缺豁免”五层机制，在不损害长尾创作者可见性的前提下抑制头部被反复推荐的马太效应；"
     "以带**阈值/窗口/基线四元组**的识别信号判定“马太在恶化”，以带**对照与显著性**的作用信号判定“是否有效”，"
     "并以**黄/红分级的量化护栏**控制过度探索、稀缺滥用等副作用，兼顾“救活长尾”与“不伤体验”这对张力。")

add_page_number(doc)
out = r"D:\learn\面试资料\tencent\马太效应抑制策略（生态治理·加分项）.docx"
doc.save(out); print("saved:", out)
